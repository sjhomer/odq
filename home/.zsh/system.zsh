# ------------------------------------------------------------------------------
# System settings
# A set of helpers to make working with the system easier.
# ------------------------------------------------------------------------------

# Bash helpers
alias editbash="cd "$HOME"/.zsh && code-insiders . && source "$HOME"/.zshrc"
alias reloadbash="source "$HOME"/.zshrc"
